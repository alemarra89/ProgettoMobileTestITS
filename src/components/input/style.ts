import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#CCC',
    borderRadius: 6,
    marginHorizontal: 20,
  },
});

export default style;
